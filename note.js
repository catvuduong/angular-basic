/**
 * 1. create angular project ng new [name], create a component - ng g c [name component]
 * 2. create a module - ng g m [name module]
 * 3. create a component of module - ng g c [component name] --module=[module name]
 * 4. in data-binding module, if u want to use ngModel, u have to import FormsModule
 * 5. ng style in attribure directive like as inline style css.
 * g directive highlight - to make 2 file of directive highlight 
 * ngNonBindable is used when we dont want to show the value
 * 6. interaction
 * @Input @Output ($event)
 * viewChild viewChildren(query list), it calls a list
 * tagMovies of viewChildren, movie tagMovies = attribute we set at list movie html.
 * inputName.value only use this method in input tag
 * 7. bus exercise
 */
